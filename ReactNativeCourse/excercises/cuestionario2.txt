More Components & Lists

Pregunta 1: How do you output a list (array) of content in React apps?
R: You use map() to map(=transform) the array of data into an array of components

Pregunta 2: Why might you need a <ScrollView> instead of a normal <View>?
R: Unlike browser, mobile apps don´t give you automatic scrolling. <ScrollView> adds it.

Pregunta 3: What's the core difference between <FlatList> and <ScrollView>?
R: FlatList optimizes scrolling by only rendering what's required